(executables
  (names driver)
  (public_names coeus)
  (libraries core cmdliner logs fmt fmt.cli fmt.tty logs.fmt logs.cli ast frontend prover searcher server)
  (preprocess (pps ppx_sexp_conv ppx_compare ppx_hash))
)
